{#
  This file is auto-generated.

  Image - The img element represents an image.

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigGenerator.php
#}
{% block img %}
{% set ariaHidden_choices = ['false', 'true'] %}
{% set ariaLabel_choices = ['breadcrumb'] %}
{% set crossorigin_choices = ['anonymous', 'use-credentials'] %}
{% set decoding_choices = ['async', 'auto', 'sync'] %}
{% set dir_choices = ['ltr', 'rtl', 'auto'] %}
{% set referrerpolicy_choices = ['no-referrer', 'no-referrer-when-downgrade', 'origin', 'origin-when-cross-origin', 'same-origin', 'strict-origin', 'strict-origin-when-cross-origin', 'unsafe-url'] %}
<img
  {% if alt is defined and alt|length > 0 %}alt="{{ alt }}"{% endif %}
  {% if ariaHidden is defined and ariaHidden|length > 0 and ariaHidden in ariaHidden_choices %}aria-hidden="{{ ariaHidden }}"{% endif %}
  {% if ariaLabel is defined and ariaLabel|length > 0 and ariaLabel in ariaLabel_choices %}aria-label="{{ ariaLabel }}"{% endif %}
  {% if class is defined and class|length > 0 %}class="{{ class }}"{% endif %}
  {% if crossorigin is defined and crossorigin|length > 0 and crossorigin in crossorigin_choices %}crossorigin="{{ crossorigin }}"{% endif %}
  {% if decoding is defined and decoding|length > 0 and decoding in decoding_choices %}decoding="{{ decoding }}"{% endif %}
  {% if dir is defined and dir|length > 0 and dir in dir_choices %}dir="{{ dir }}"{% endif %}
  {% if draggable is defined and draggable|length > 0 %}draggable="{{ draggable }}"{% endif %}
  {% if height is defined and height|length > 0 %}height="{{ height }}"{% endif %}
  {% if hidden is defined and hidden|length > 0 %}hidden="{{ hidden }}"{% endif %}
  {% if id is defined and id|length > 0 %}id="{{ id }}"{% endif %}
  {% if ismap is defined and ismap|length > 0 %}ismap="{{ ismap }}"{% endif %}
  {% if lang is defined and lang|length > 0 %}lang="{{ lang }}"{% endif %}
  {% if referrerpolicy is defined and referrerpolicy|length > 0 and referrerpolicy in referrerpolicy_choices %}referrerpolicy="{{ referrerpolicy }}"{% endif %}
  {% if sizes is defined and sizes|length > 0 %}sizes="{{ sizes }}"{% endif %}
  {% if src is defined and src|length > 0 %}src="{{ src }}"{% endif %}
  {% if srcset is defined and srcset|length > 0 %}srcset="{{ srcset }}"{% endif %}
  {% if style is defined and style|length > 0 %}style="{{ style }}"{% endif %}
  {% if tabindex is defined and tabindex|length > 0 %}tabindex="{{ tabindex }}"{% endif %}
  {% if title is defined and title|length > 0 %}title="{{ title }}"{% endif %}
  {% if usemap is defined and usemap|length > 0 %}usemap="{{ usemap }}"{% endif %}
  {% if width is defined and width|length > 0 %}width="{{ width }}"{% endif %}/>
{% endblock %}
