{#
  This file is auto-generated.

  Input - The input element represents a typed data field, usually with a form control to allow user input.

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigGenerator.php
#}
{% block input %}
{% set ariaCurrent_choices = ['false', 'page', 'step', 'location', 'date', 'time', 'true'] %}
{% set ariaDisabled_choices = ['false', 'true'] %}
{% set ariaInvalid_choices = ['false', 'true', 'grammar', 'spelling'] %}
{% set autocapitalize_choices = ['none', 'sentences', 'words', 'characters'] %}
{% set autocomplete_choices = ['off', 'on'] %}
{% set autocorrect_choices = ['off', 'on'] %}
{% set contenteditable_choices = ['true', 'false', 'inherit'] %}
{% set dir_choices = ['ltr', 'rtl', 'auto'] %}
{% set formenctype_choices = ['application/x-www-form-urlencoded', 'multipart/form-data', 'text/plain'] %}
{% set formmethod_choices = ['get', 'post', 'dialog'] %}
{% set formtarget_choices = ['_self', '_blank', '_parent', '_top'] %}
{% set inputmode_choices = ['none', 'text', 'decimal', 'numeric', 'email', 'tel', 'url', 'search'] %}
{% set popovertargetaction_choices = ['show', 'hide', 'toggle'] %}
{% set role_choices = ['alert', 'application', 'article', 'banner', 'button', 'checkbox', 'complementary', 'contentinfo', 'dialog', 'form', 'grid', 'group', 'heading', 'img', 'link', 'list', 'listbox', 'listitem', 'main', 'menu', 'menubar', 'menuitem', 'navigation', 'none', 'presentation', 'radio', 'region', 'search', 'status', 'tab', 'tablist', 'tabpanel', 'textbox', 'toolbar', 'tooltip'] %}
{% set spellcheck_choices = ['true', 'false'] %}
{% set translate_choices = ['yes', 'no'] %}
{% set type_choices = ['button', 'checkbox', 'color', 'date', 'datetime-local', 'email', 'file', 'hidden', 'image', 'month', 'number', 'password', 'radio', 'range', 'reset', 'search', 'submit', 'tel', 'text', 'time', 'url', 'week'] %}
<input
  {% if accept is defined and accept|length > 0 %}accept="{{ accept }}"{% endif %}
  {% if accesskey is defined and accesskey|length > 0 %}accesskey="{{ accesskey }}"{% endif %}
  {% if alt is defined and alt|length > 0 %}alt="{{ alt }}"{% endif %}
  {% if ariaControls is defined and ariaControls|length > 0 %}aria-controls="{{ ariaControls }}"{% endif %}
  {% if ariaCurrent is defined and ariaCurrent|length > 0 and ariaCurrent in ariaCurrent_choices %}aria-current="{{ ariaCurrent }}"{% endif %}
  {% if ariaDescribedby is defined and ariaDescribedby|length > 0 %}aria-describedby="{{ ariaDescribedby }}"{% endif %}
  {% if ariaDisabled is defined and ariaDisabled|length > 0 and ariaDisabled in ariaDisabled_choices %}aria-disabled="{{ ariaDisabled }}"{% endif %}
  {% if ariaInvalid is defined and ariaInvalid|length > 0 and ariaInvalid in ariaInvalid_choices %}aria-invalid="{{ ariaInvalid }}"{% endif %}
  {% if ariaLabel is defined and ariaLabel|length > 0 %}aria-label="{{ ariaLabel }}"{% endif %}
  {% if ariaLabelledby is defined and ariaLabelledby|length > 0 %}aria-labelledby="{{ ariaLabelledby }}"{% endif %}
  {% if autocapitalize is defined and autocapitalize|length > 0 and autocapitalize in autocapitalize_choices %}autocapitalize="{{ autocapitalize }}"{% endif %}
  {% if autocomplete is defined and autocomplete|length > 0 and autocomplete in autocomplete_choices %}autocomplete="{{ autocomplete }}"{% endif %}
  {% if autocorrect is defined and autocorrect|length > 0 and autocorrect in autocorrect_choices %}autocorrect="{{ autocorrect }}"{% endif %}
  {% if autofocus is defined and autofocus|length > 0 %}autofocus="{{ autofocus }}"{% endif %}
  {% if checked is defined and checked|length > 0 %}checked="{{ checked }}"{% endif %}
  {% if class is defined and class|length > 0 %}class="{{ class }}"{% endif %}
  {% if contenteditable is defined and contenteditable|length > 0 and contenteditable in contenteditable_choices %}contenteditable="{{ contenteditable }}"{% endif %}
  {% if dir is defined and dir|length > 0 and dir in dir_choices %}dir="{{ dir }}"{% endif %}
  {% if dirname is defined and dirname|length > 0 %}dirname="{{ dirname }}"{% endif %}
  {% if disabled is defined and disabled|length > 0 %}disabled="{{ disabled }}"{% endif %}
  {% if draggable is defined and draggable|length > 0 %}draggable="{{ draggable }}"{% endif %}
  {% if form is defined and form|length > 0 %}form="{{ form }}"{% endif %}
  {% if formaction is defined and formaction|length > 0 %}formaction="{{ formaction }}"{% endif %}
  {% if formenctype is defined and formenctype|length > 0 and formenctype in formenctype_choices %}formenctype="{{ formenctype }}"{% endif %}
  {% if formmethod is defined and formmethod|length > 0 and formmethod in formmethod_choices %}formmethod="{{ formmethod }}"{% endif %}
  {% if formnovalidate is defined and formnovalidate|length > 0 %}formnovalidate="{{ formnovalidate }}"{% endif %}
  {% if formtarget is defined and formtarget|length > 0 and formtarget in formtarget_choices %}formtarget="{{ formtarget }}"{% endif %}
  {% if height is defined and height|length > 0 %}height="{{ height }}"{% endif %}
  {% if hidden is defined and hidden|length > 0 %}hidden="{{ hidden }}"{% endif %}
  {% if id is defined and id|length > 0 %}id="{{ id }}"{% endif %}
  {% if inputmode is defined and inputmode|length > 0 and inputmode in inputmode_choices %}inputmode="{{ inputmode }}"{% endif %}
  {% if lang is defined and lang|length > 0 %}lang="{{ lang }}"{% endif %}
  {% if list is defined and list|length > 0 %}list="{{ list }}"{% endif %}
  {% if max is defined and max|length > 0 %}max="{{ max }}"{% endif %}
  {% if maxlength is defined and maxlength|length > 0 %}maxlength="{{ maxlength }}"{% endif %}
  {% if min is defined and min|length > 0 %}min="{{ min }}"{% endif %}
  {% if minlength is defined and minlength|length > 0 %}minlength="{{ minlength }}"{% endif %}
  {% if multiple is defined and multiple|length > 0 %}multiple="{{ multiple }}"{% endif %}
  {% if name is defined and name|length > 0 %}name="{{ name }}"{% endif %}
  {% if pattern is defined and pattern|length > 0 %}pattern="{{ pattern }}"{% endif %}
  {% if placeholder is defined and placeholder|length > 0 %}placeholder="{{ placeholder }}"{% endif %}
  {% if popovertarget is defined and popovertarget|length > 0 %}popovertarget="{{ popovertarget }}"{% endif %}
  {% if popovertargetaction is defined and popovertargetaction|length > 0 and popovertargetaction in popovertargetaction_choices %}popovertargetaction="{{ popovertargetaction }}"{% endif %}
  {% if readonly is defined and readonly|length > 0 %}readonly="{{ readonly }}"{% endif %}
  {% if required is defined and required|length > 0 %}required="{{ required }}"{% endif %}
  {% if role is defined and role|length > 0 and role in role_choices %}role="{{ role }}"{% endif %}
  {% if size is defined and size|length > 0 %}size="{{ size }}"{% endif %}
  {% if spellcheck is defined and spellcheck|length > 0 and spellcheck in spellcheck_choices %}spellcheck="{{ spellcheck }}"{% endif %}
  {% if src is defined and src|length > 0 %}src="{{ src }}"{% endif %}
  {% if step is defined and step|length > 0 %}step="{{ step }}"{% endif %}
  {% if style is defined and style|length > 0 %}style="{{ style }}"{% endif %}
  {% if tabindex is defined and tabindex|length > 0 %}tabindex="{{ tabindex }}"{% endif %}
  {% if title is defined and title|length > 0 %}title="{{ title }}"{% endif %}
  {% if translate is defined and translate|length > 0 and translate in translate_choices %}translate="{{ translate }}"{% endif %}
  {% if type is defined and type|length > 0 and type in type_choices %}type="{{ type }}"{% endif %}
  {% if value is defined and value|length > 0 %}value="{{ value }}"{% endif %}
  {% if width is defined and width|length > 0 %}width="{{ width }}"{% endif %}/>
{% endblock %}
